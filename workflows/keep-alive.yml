name: Keep Render Backend Awake

on:
  schedule:
    - cron: '*/6 * * * *' # Every 6 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: curl -s https://rctcetbackend.onrender.com > /dev/null
